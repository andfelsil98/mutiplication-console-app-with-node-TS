{
  "name": "04-multiplication",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx ts-node src/app.ts -b 6 -n test-table -d test/tables",
    "dev:logic": "npx ts-node src/app.logic.ts -b 10 -s -l 100",
    "dev:nodemon": "npx nodemon -b 10",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/yargs": "^17.0.33",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "yargs": "^17.7.2"
  }
}
